function removeDuplicateLetters(s: string): string {
    let ar = Array<Array<number>>(26)
    const a = 'a'.charCodeAt(0);
    for (;;) {

    }

    let r = ''
    return r
};

console.log(removeDuplicateLetters("cbacdcbc"));
